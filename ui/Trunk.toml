[build]
target = "index.html"
dist = "dist"
public_url = "/"

[serve]
port = 1420
open = false

[[hooks]]
stage = "pre_build"
command = "sh"
command_arguments = ["-c", "npx tailwindcss -i ./styles/main.css -o ./styles/output.css 2>/dev/null || echo 'Tailwind not installed, using fallback'"]
